<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.werkws18_21.einkaufsliste">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_liste"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_liste_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Accounterstellen" />
        <activity android:name=".Liste" />
        <activity android:name=".Gruppenauswahl" />
        <activity android:name=".AccountSettings" />
        <activity android:name=".GruppenManager" />
        <activity android:name=".Gps"></activity>
    </application>

</manifest>